import React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
    <Layout>
        <div>
            <h1>没有找到</h1>
            <p>你访问的页面不存在</p>
        </div>
    </Layout>
);

export default NotFoundPage;
