import React from "react";

import Layout from "../../components/Layout";
import { PreviewProps } from "../../common/interfaces";

const UnitsSettingsPreview: React.SFC<PreviewProps> = ({}) => <Layout />;

export default UnitsSettingsPreview;
